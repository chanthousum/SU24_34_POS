version: '3.9'
services:
  db:
    image: postgres:15
    container_name: "postgres_su24_34_pos_db"
    restart: always
    environment:
      POSTGRES_DB: su24_34_pos_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Password123@
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5439:5432"
  web:
    container_name: "su24_34_django_app"
    build: .
    restart: always
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - db
volumes:
  postgres_data:
